# frozen_string_literal: true

module Observatory

  # The engine's released version, read by the gemspec and reported in the
  # dashboard footer so an operator can tell which build produced a trace.
  #
  VERSION = "0.1.0".freeze
end
